import * as actions from './actions/index';
import store from './store';

store.dispatch(actions.Hot_Response('Hot'));
console.log(store.getState());

for(let i=0; i<numbers.length; i++) {
	const number = i;
	if(val < 6) {
		return guess
	}
}


const Fetch_Fewest_Guesses => dispatch => {
	return fetch.then(response => {
	}
	return response;
})
	.then(response => .json())
	.then(data =>
		dispatch(fetchFewestGuesses(Fewest-Guesses, Number))
	)

const Save_Fewest_Guesses => dispatch => {
	return push.then(response => {
	}
	return response;
}
.then(response => .json())
.then(data =>
	dispatch(Save_Fewest_Guesses(Fewest_Guesses, Number)
	)